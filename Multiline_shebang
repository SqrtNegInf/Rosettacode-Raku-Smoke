#!/usr/bin/env perl6 do-not-check
#u# http://rosettacode.org/wiki/Multiline_shebang
#c# 2018-04-10 <RC
#t# broken
#m# MOAR: BROKEN
#j# JVM:  BROKEN

# This example is broken. It fails to compile or causes a runtime error. Please fix the code and remove this message.

#`{{

#!/usr/local/bin/perl6
eval '(exit $?0)' && eval 'exec perl6 $0 ${1+"[email protected]"}'
& eval 'exec perl6 $0 $argv:q'
        if 0;

}}
