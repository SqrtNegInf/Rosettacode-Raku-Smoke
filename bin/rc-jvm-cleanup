#!/usr/bin/env perl6
## store JVM results and zap directory & link
#    2021-03-20

exit unless 'bench/jvm'.IO ~~ :e;
shell "rakubrew switch moar-blead; cd bench; mv jvm/* jlast; rmdir {Date.today}-j; rm jvm";
